@extends('layouts.institute')
@section('title','Return / Refund Report — AccumenAI')
@section('content')
<div class="d-flex justify-content-between align-items-center mb-3"><h4 class="mb-0">Return / Refund Report</h4><div class="d-flex gap-2"><a href="{{ request()->fullUrlWithQuery(['export'=>'csv']) }}" class="btn btn-sm btn-outline-success rounded-pill"><i class="bi bi-download"></i> CSV</a><button class="btn btn-sm btn-outline-primary rounded-pill" onclick="window.print()"><i class="bi bi-printer"></i> Print</button><a href="{{ route('sales.reports.dashboard') }}" class="btn btn-sm btn-outline-secondary rounded-pill">Dashboard</a></div></div>
<form method="GET" class="card mb-3"><div class="card-body d-flex flex-wrap gap-2 align-items-end"><div><label class="form-label small mb-1">From</label><input type="date" name="from" value="{{ $filters['from'] }}" class="form-control form-control-sm"></div><div><label class="form-label small mb-1">To</label><input type="date" name="to" value="{{ $filters['to'] }}" class="form-control form-control-sm"></div><div><label class="form-label small mb-1">Customer ID</label><input type="number" name="customer_id" value="{{ $filters['customer_id'] }}" class="form-control form-control-sm" style="width:120px"></div><button class="btn btn-sm btn-primary rounded-pill">Filter</button><a href="{{ route('sales.reports.returns') }}" class="btn btn-sm btn-outline-secondary rounded-pill">Reset</a></div></form>
<div class="card mb-3"><div class="card-header">Summary by Date</div><div class="table-responsive"><table class="table mb-0"><thead><tr><th>Date</th><th class="text-end">Returns</th><th class="text-end">Total</th><th class="text-end">Refunded</th></tr></thead><tbody>@forelse($rows as $r)<tr><td>{{ $r->period }}</td><td class="text-end">{{ $r->count }}</td><td class="text-end">{{ number_format($r->total,2) }}</td><td class="text-end">{{ number_format($r->refunded,2) }}</td></tr>@empty<tr><td colspan="4" class="text-center text-muted py-3">No returns.</td></tr>@endforelse</tbody></table></div></div>
<div class="card"><div class="card-header">Recent Returns (posted)</div><div class="table-responsive"><table class="table mb-0"><thead><tr><th>Return</th><th>Credit Note</th><th>Customer</th><th>Date</th><th class="text-end">Total</th><th>Refund</th><th>Status</th></tr></thead><tbody>@forelse($detail as $ret)<tr><td>{{ $ret->return_number }}</td><td>{{ $ret->credit_note_number }}</td><td>{{ $ret->customer?->name }}</td><td>{{ $ret->return_date->format('Y-m-d') }}</td><td class="text-end">{{ number_format($ret->grand_total,2) }}</td><td class="text-end">{{ number_format($ret->refunded_amount,2) }}</td><td><span class="badge bg-success">{{ $ret->status }}</span></td></tr>@empty<tr><td colspan="7" class="text-center text-muted py-3">No detail.</td></tr>@endforelse</tbody></table></div><div class="card-footer">{{ $detail->links() }}</div></div>
@endsection
